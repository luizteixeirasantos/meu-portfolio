// Importamos a configuração base do nosso pacote de config
const baseConfig = require("@luiz/config/tailwind.config.js");

module.exports = {
  ...baseConfig,
  // Podemos adicionar overrides específicos para a web se necessário aqui
};
